#include <stdlib.h>
#include <stdio.h>
#include <string.h>

// Function declaration

char* decimal_to_binary(char , int *, char* );
char* reverse_char_array(char *, int *, char* );
char * character_to_binary(char );
char * add_zeros_to_binary_rpztation(char *, int *);